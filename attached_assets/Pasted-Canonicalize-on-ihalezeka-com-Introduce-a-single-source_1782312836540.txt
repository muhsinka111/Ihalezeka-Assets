Canonicalize on ihalezeka.com — Introduce a single source of truth for the public base URL on the server (env var defaulting to https://ihalezeka.com) and use it in the sitemap, blog SSR, and robots route. Update index.html Open Graph URL, JSON-LD @id/url/publisher URL, and add a <link rel="canonical"> to use ihalezeka.com.
Fix social share cards in index.html — Add og:image and twitter:image pointing to the hosted brand image, plus og:image:width/height/alt, og:locale (tr_TR), og:url, and theme-color. Regenerate the share image to 1200×630 (current is 1280×720).
robots.txt + sitemap correctness — Serve robots.txt reliably in production (Express route) with a Sitemap: line and disallow rules for /api, admin, and auth-gated app paths. Rewrite the sitemap to use the canonical domain and include ONLY public indexable URLs (home, /blog, blog posts, new public marketing pages); remove /dashboard, /ihale-arama, /firsatlarim. Add lastmod where available.
Per-page meta for public SPA pages — Add a head/meta manager (e.g. react-helmet-async) and give each public page (landing + new marketing pages) a unique title, meta description, canonical, and OG/Twitter tags. Authenticated app pages can keep generic meta.
International tender brand positioning (crawlable) — Add a public marketing page (e.g. /uluslararasi-ihaleler) and a landing-page section that markets coverage of World Bank, EU/TED, EBRD, UN/UNGM, AIIB, and IsDB tenders under the İhaleZeka brand, with an enterprise "yakında" framing. Ensure it is crawlable (reuse the existing SSR blog shell pattern for a server-rendered version if needed) and add it to the sitemap. Target Turkish keywords like "uluslararası ihale takip", "Dünya Bankası ihaleleri".
Brand structured data + real footer social links — Add an Organization node (name, logo, url, and an empty-ready sameAs slot) to the JSON-LD graph alongside the existing WebSite/SoftwareApplication. Convert the footer's dead Twitter/LinkedIn/Instagram text into real anchor links wired to a small config object (left empty/hidden until the user provides URLs). Add social share buttons on blog posts.
Verify — Confirm robots.txt and sitemap.xml return correct content types and bodies (not the SPA HTML fallback), validate JSON-LD shape, and confirm OG/Twitter tags resolve to an absolute hosted image. Note: full social-card preview validation (Facebook/X debuggers) requires the live deployed domain.
